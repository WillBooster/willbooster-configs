# oxfmt-config

An Oxfmt config file for my repositories.
Install the package and its peer dependency.

```sh
yarn add -D @willbooster/oxfmt-config oxfmt
```

Then pass the shared config file to Oxfmt.

```sh
oxfmt -c ./node_modules/@willbooster/oxfmt-config/.oxfmtrc.json .
```
